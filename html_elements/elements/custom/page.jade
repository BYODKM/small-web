mixin page

  //-
    - Usage: +page
    - Yield: .page.page--dirname-dirname-filename

  -
    var curl = attributes.curl
    attributes.curl = null

  div&attributes(attributes).page(class="page--#{__page}")
    block

    if curl
      p(class="page__curl")
        a(href=curl)=curl
